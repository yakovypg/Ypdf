<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\.editorconfig" Link=".editorconfig" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="itext7" Version="7.2.3" />
    <PackageReference Include="Mono.Options" Version="6.12.0.148" />
  </ItemGroup>

  <Target Name="CopyPythonTools" AfterTargets="AfterBuild">
	<Copy SourceFiles="../PythonTools/ImageExtractor.py" DestinationFolder="$(OutDir)" />
  </Target>
  <Target Name="CopyPythonToolsOnPublish" AfterTargets="Publish">
	<Copy SourceFiles="../PythonTools/ImageExtractor.py" DestinationFolder="$(PublishDir)" />
  </Target>

</Project>
